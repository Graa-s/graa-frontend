import Property from './Property';
import { PropertyTitle, PropertyValue } from './Property.styled';
import type { Props as PropertyProps } from './Property';

export default Property;
export { PropertyTitle, PropertyValue };
export type { PropertyProps };
