import styled from 'styled-components';

export default styled.div`
  position: absolute;
  top: 10px;
  right: 10px;
  padding: 9px 10px;
  padding: 6px 7px;
  display: flex;
  align-items: center;
  gap: 3px;
  background-color: rgba(0, 0, 0, 0.5);
  border-radius: 10px;
  font-size: 14px;
  font-weight: 600;
  color: white;
`;
